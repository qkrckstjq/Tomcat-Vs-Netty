spring:
  application:
    name: tomcat

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tomcatnetty
    username: root
    password: PCs14025487

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
